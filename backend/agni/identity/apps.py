from django.apps import AppConfig


class IdentityConfig(AppConfig):
    name = "agni.identity"
    label = "identity"
    verbose_name = "Agni Setu identity"
    default_auto_field = "django.db.models.BigAutoField"
